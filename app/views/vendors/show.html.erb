<h1 class="font-bold">Vendor details</h1>
<div>
    <%= button_to "Delete", vendor_path(@vendor), method: :delete, data: { turbo_confirm: "Are you sure?" } %>
    <%= link_to "Edit", edit_vendor_path(@vendor) %>
</div>
<h2>Name: <%= @vendor.name %></h2>
<div>Country: <%= @vendor.country %></div>